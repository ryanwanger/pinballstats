<%= form_for(@group) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :league_night_id %><br>
    <%= collection_select(:group, :league_night_id, LeagueNight.all.order("date desc"), :id, :dropdown_title) %>
  </div>
  <div class="field">
    <%= f.label :group_rank %><br>
    <%= f.number_field :group_rank %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
